<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.yeast.mappers.company.MyPageCMapper">
    <select id="selectCMemberById" resultType="com.kh.yeast.domain.entity.Business">
        SELECT MEMBER m, b.BUSINESS_NAME AS businessName
        FROM MEMBER m
        LEFT JOIN BUSINESS b ON m.BUSINESS_NO = b.BUSINESS_NO
        WHERE m.USER_ID = #{userId}
    </select>
    <select id="selectCMemberByUserNo" resultType="com.kh.yeast.domain.entity.Business">
        SELECT MEMBER m, b.BUSINESS_NAME AS businessName
        FROM MEMBER m
        LEFT JOIN BUSINESS b ON m.BUSINESS_NO = b.BUSINESS_NO
        WHERE m.USER_NO = #{userNo}
    </select>
    <update id="updateCMember">
        UPDATE members
        SET
            EMAIL = #{email},
            USER_NAME = #{userName},
            PASSWORD = #{password},
            ADDRESS = #{address},
            PHONE = #{phone},
            BIRTHDAY = #{birthdate},
            POSITION = #{position},
            BUSINESS = #{business},
            GENDER = #{gender}
        WHERE USER_ID = #{userId}
    </update>
</mapper>